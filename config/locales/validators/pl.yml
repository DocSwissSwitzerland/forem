pl:
  validators:
    bytesize_validator:
      non_negative: :maximum musi być nieujemną liczbą całkowitą
    color_contrast_validator:
      must_be_darker: musi być ciemniejszy w celu ułatwienia dostępu
    emoji_only_validator:
      invalid_emoji: zawiera znaki inne niż emoji lub niepoprawne emoji
    existing_published_article_id_validator:
      default: musi być poprawnym opublikowanym identyfikatorem art.
    profile_validator:
      too_long: jest za długie
    unique_cross_model_slug_validator:
      is_taken: jest przyjmowany
    valid_domain_csv_validator:
      invalid_list_format: musi być listą rozdzielonych przecinkami poprawnych domen.

