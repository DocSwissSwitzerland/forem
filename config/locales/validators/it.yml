it:
  validators:
    bytesize_validator:
      non_negative: :massimo deve essere un Integer non negativo
    color_contrast_validator:
      must_be_darker: deve essere darker per l'accessibilità
    emoji_only_validator:
      invalid_emoji: contiene caratteri non emoji o emoji non validi
    existing_published_article_id_validator:
      default: deve essere un identificativo dell'articolo pubblicato valido
    profile_validator:
      too_long: è troppo lungo
    unique_cross_model_slug_validator:
      is_taken: è preso
    valid_domain_csv_validator:
      invalid_list_format: deve essere un elenco separato da virgole di domini validi

